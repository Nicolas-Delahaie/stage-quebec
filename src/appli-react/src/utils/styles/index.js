/**
 * Fichier permetant de regrouper les styles de l'application afin d'importer qu'un seul fichier dans les composants
 */

import { fonts } from "./fonts";
import { colors } from "./colors";

export { fonts, colors }
